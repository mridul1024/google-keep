import { makeStyles } from "@material-ui/styles";

export const useStyles = makeStyles({
  header: {
    letterSpacing: "2px",
    fontWeight: "lighter",
    textTransform: "uppercase",
  },
  appBar: {
    background: "#fff59d",
    color: "black",
  },
});
